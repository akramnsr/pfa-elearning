DROP TABLE IF EXISTS quizzes;
CREATE TABLE quizzes (
                         id BIGINT AUTO_INCREMENT PRIMARY KEY,
                         titre VARCHAR(255),
                         description VARCHAR(255)
    -- ajoutez ici toutes vos colonnes réelles
);
